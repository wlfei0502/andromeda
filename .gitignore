/target

.worktrees/

.superpowers/

# Cursor IDE (local rules / agent state — not project docs under docs/superpowers/)
.cursor/
.cursorignore
*.code-workspace
.agent/

# Local secrets — copy from config.example.toml
config.toml

# Long-horizon run checkpoints (LocalFsRunStore)
/data/
